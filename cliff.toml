[changelog]
# A Tera template to be rendered as the changelog's header.
# See https://keats.github.io/tera/docs/#introduction
header = """
# Changelog\n
All notable changes to this project will be documented in this file.\n
"""
# A Tera template to be rendered for each release in the changelog.
# See https://keats.github.io/tera/docs/#introduction
body = """
{% if version %}\
    ## {{ version | trim_start_matches(pat="v") }} - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## Unreleased
{% endif %}\
{% if previous %}\
    {% if previous.commit_id and commit_id %}
        [{{ previous.commit_id | truncate(length=7, end="") }}]({{ previous.commit_id }})...\
            [{{ commit_id | truncate(length=7, end="") }}]({{ commit_id }})
    {% endif %}\
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper_first }}
    {% for commit in commits %}
        - {{ commit.message | split(pat="\n") | first | upper_first | trim }} ([{{ commit.id | truncate(length=7, end="") }}]({{ commit.id }}))\
          {% if commit.footers %}\
            {% for footer in commit.footers -%}
                , {{ footer.token }}{{ footer.separator }}{{ footer.value }}\
            {% endfor %}\
          {% endif %}\
    {% endfor %}
{% endfor %}\n
"""
# A Tera template to be rendered as the changelog's footer.
# See https://keats.github.io/tera/docs/#introduction
footer = """
<!-- generated by git-cliff -->
"""
# Remove leading and trailing whitespaces from the changelog's body.
trim = true

[git]
# Parse commits according to the conventional commits specification.
# See https://www.conventionalcommits.org
conventional_commits = true
# Exclude commits that do not match the conventional commits specification.
filter_unconventional = false
# An array of regex based parsers for extracting data from the commit message.
# Assigns commits to groups.
# Optionally sets the commit's scope and can decide to exclude commits from further processing.
commit_parsers = [
    # Features
    { message = "^:feat:|^feat:", group = "🚀 Features" },

    # Fixes
    { message = "^:fix:|^fix:", group = "🐛 Bug Fixes" },

    # Documentation
    { message = "^:readme:|^:yipee:|^docs:|^fix-docs:|^readme-docs:", group = "📚 Documentation" },

    # Performance
    { message = "^:sys_imv:|^enhancement:", group = "⚡ Performance & Improvements" },

    # Packages
    { message = "^:pkg\\(|^add_pkg:|^remove_unused_pg:|^up:|^fish:", group = "📦 Packages" },

    # CLI
    { message = "^:cli:", group = "🖥️ CLI" },

    # Tests
    { message = "^:kawaii:|^test:", group = "🧪 Testing" },

    # Memory leaks
    { message = "^:water:", group = "💧 Memory Fixes" },

    # Cleanup
    { message = "^:fire:", group = "🚜 Cleanup" },

    # Reverts
    { message = "^revert", group = "◀️ Reverts" },

    # Security
    { body = ".*security", group = "🛡️ Security" },

    # Everything else
    { message = ".*", group = "💼 Other" },
]
# Exclude commits that are not matched by any commit parser.
filter_commits = false
# Order releases topologically instead of chronologically.
topo_order = false
# Order of commits in each group/release within the changelog.
# Allowed values: newest, oldest
sort_commits = "oldest"